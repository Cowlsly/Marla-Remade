name: "Precise Issue App Labeler"
on:
  issues:
    types: [opened]

jobs:
  add-labels:
    runs-on: ubuntu-latest
    permissions:
      issues: write
    steps:
      - name: Extract App Name and Label
        uses: actions/github-script@v7
        with:
          script: |
            const body = context.payload.issue.body;

            // This regex finds the text immediately following the "### App Name" header
            // It accounts for potential whitespace and newlines
            const match = body.match(/### App Name\s+([\s\S]*?)(?:\n\n|###|$)/);

            if (match && match[1]) {
              const selectedApp = match[1].trim().toLowerCase();
              const validApps = ['alchemist', 'appstore', 'astronomy', 'calculator', 'calendar', 'camera', 'chess', 'clock', 'code', 'contacts', 'education', 'email', 'everysync', 'files', 'findfamily', 'fooddelivery', 'health', 'hub', 'keyboard', 'launcher', 'logicgate', 'maps', 'measure', 'messages', 'music', 'musicbrainz', 'notes', 'office', 'openassistant', 'passwords', 'pdf', 'photos', 'pipes', 'solitaire', 'speech', 'things', 'translate', 'travel', 'unblockjam', 'vpn', 'voxels', 'weather', 'web', 'wordmaker', 'youpipe'];

              if (validApps.includes(selectedApp)) {
                await github.rest.issues.addLabels({
                  issue_number: context.issue.number,
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  labels: [selectedApp]
                });
                console.log(`Successfully added label: ${selectedApp}`);
              }
            }
