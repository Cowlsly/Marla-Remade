<?xml version="1.0" encoding="utf-8"?>
<!--
  Cast. Audited state: none. The device list is rebuilt by mDNS on every launch, the
  session lives only as long as the TLS socket, and the file the user casts is read
  straight from its content:// URI - nothing is copied into app storage. Nothing to
  exclude, so back everything up.
-->
<data-extraction-rules>
    <cloud-backup>
        <include domain="root" path="." />
        <include domain="file" path="." />
        <include domain="database" path="." />
        <include domain="sharedpref" path="." />
        <include domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <include domain="root" path="." />
        <include domain="file" path="." />
        <include domain="database" path="." />
        <include domain="sharedpref" path="." />
        <include domain="external" path="." />
    </device-transfer>
</data-extraction-rules>
