<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.BACKUP" />
    <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <!--
      Guards the settings UI so only the system can open it. Backup has no launcher icon -
      it is reached from Settings > System > Backup, which starts it via the transport's
      dataManagementIntent - and an exported activity with no guard would let any installed
      app open the backup configuration.
    -->
    <permission
        android:name="com.vayunmathur.backup.OPEN_SETTINGS"
        android:protectionLevel="signature" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.General">
        <!--
          No MAIN/LAUNCHER filter: backup is configured from Settings, not from a home-screen
          icon, the same way Seedvault is on GrapheneOS. The framework reaches this activity
          through ConfigurableBackupTransport.dataManagementIntent(), and Settings' backup page
          renders it under the label the transport supplies.
        -->
        <activity
            android:configChanges="mcc|mnc|colorMode|density|fontScale|fontWeightAdjustment|keyboard|keyboardHidden|layoutDirection|navigation|orientation|screenLayout|screenSize|smallestScreenSize|touchscreen|uiMode"
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:permission="com.vayunmathur.backup.OPEN_SETTINGS"
            android:theme="@style/Theme.General" />

        <service
            android:name=".service.ConfigurableBackupTransportService"
            android:exported="true"
            android:permission="android.permission.BIND_BACKUP_TRANSPORT">
            <intent-filter>
                <action android:name="android.backup.TRANSPORT_HOST" />
            </intent-filter>
        </service>
    </application>

</manifest>
