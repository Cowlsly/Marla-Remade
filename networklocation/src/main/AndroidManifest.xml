<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INSTALL_LOCATION_PROVIDER" />
    <uses-permission android:name="android.permission.LOCATION_HARDWARE" />
    <uses-permission android:name="android.permission.MODIFY_PHONE_STATE" />
    <uses-permission android:name="android.permission.UPDATE_DEVICE_STATS" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.General">

        <uses-library android:name="com.android.location.provider" android:required="false" />

        <service
            android:name=".NetworkLocationService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.android.location.service.v3.NetworkLocationProvider" />
            </intent-filter>
        </service>

        <service
            android:name=".GeocodeService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.android.location.service.GeocodeProvider" />
            </intent-filter>
        </service>

        <!--
          No launcher entry: the app is headless and its status screen is reached from
          Settings -> Location -> Location services. Exported because Settings is another
          package, but restricted to the platform, which is what Settings runs as.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:permission="android.permission.INSTALL_LOCATION_PROVIDER" />
        <provider
            android:name=".provider.DatabaseStatusProvider"
            android:authorities="com.vayunmathur.networklocation.status"
            android:exported="true"
            android:readPermission="android.permission.INSTALL_LOCATION_PROVIDER" />
    </application>

</manifest>
