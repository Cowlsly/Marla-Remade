<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- Cross-app orderable-lookup guard (P19): the exported OrderLookupProvider
         is readable only by apps signed with the same key (the maps app), which
         is why this is signature-level. On any other signer it is not granted and
         the provider is simply unreadable. -->
    <permission
        android:name="com.vayunmathur.fooddelivery.permissions.ACCESS_ORDERS"
        android:protectionLevel="signature" />

    <application
        android:name=".FoodDeliveryApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.General">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.General">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Cross-app deep link (P19): fooddelivery://restaurant/<id> opens
                 that merchant's order page. Fired by the maps place-sheet "Order"
                 button (see OrderLookupContract.deepLink). -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="fooddelivery" android:host="restaurant" />
            </intent-filter>
        </activity>

        <!-- Cross-app orderable lookup (P19): tolerant name+proximity match over
             the merchant catalog, returning { orderable, restaurantId, deepLink }.
             Exported but signature-guarded so only co-signed apps (maps) can read. -->
        <provider
            android:name=".ipc.OrderLookupProvider"
            android:authorities="com.vayunmathur.fooddelivery.orderlookup"
            android:exported="true"
            android:permission="com.vayunmathur.fooddelivery.permissions.ACCESS_ORDERS" />

        <service
            android:name=".notifications.OrderTrackingService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="order_tracking" />
        </service>
    </application>

</manifest>
